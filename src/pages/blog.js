import React from 'react'
import Layout from '../components/layout'

const Blog = ({location}) => (
    <Layout location={location}>
        <div>
            <h1>Blog</h1>
        </div>
    </Layout>
)

export default Blog