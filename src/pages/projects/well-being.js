import React from 'react';

import Layout from '../../components/layout.js';

const WellBeing = ({location}) => (
    <Layout location={location}>
        <h1>Measuring Well-Being for a better World</h1>
    </Layout>
);

export default WellBeing;